# FinPilot verified build

Use ONLY these two files.

1. Terminal 1: `ollama run qwen3:8b`
2. Terminal 2: `cd /path/to/this/folder && python3 server.py`
3. Open `http://127.0.0.1:8000/FinPilot.html`

This build shows `BUILD · 2026.09.17` under the FinPilot sidebar brand. If that marker is not visible, the browser is loading a different/old FinPilot.html.

Accounts persist in `~/.finpilot/finpilot.db`.
